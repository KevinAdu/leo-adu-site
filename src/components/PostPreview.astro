---
import { Image } from 'astro:assets';

export interface Props {
	post: any;
}

const { post } = Astro.props;
---

<article class="post-preview">
	<img src={post.frontmatter.photo} alt={post.frontmatter.title} loading="lazy" />
</article>
